name: best_matcher
description: A new Flutter project.

publish_to: 'none'

version: 0.0.1+1

environment:
  sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cloud_firestore: ^4.4.5
  firebase_core: ^2.8.0
  firebase_auth: ^4.3.0
  provider: ^6.0.0
  english_words: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  cloud_firestore: ^4.4.5
  firebase_core: ^2.8.0
  firebase_auth: ^4.3.0
  english_words: ^4.0.0
  provider: ^6.0.0

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true